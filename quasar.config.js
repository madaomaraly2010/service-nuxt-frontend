const { build } = require("nuxt")

module.exports = function (ctx) { // can be async too (@quasar/app v2.1+)
    // Example output on console:
    {
    build:{
        rtl:true
    }
    }
  
    // context gets generated based on the parameters
    // with which you run 
}